#include <Python.h>

// Placeholder translation unit for the MC basic native target. The first
// executable Phase 5 slice keeps LoS in Torch CUDA while the native op surface
// is expanded in later RayDN linkage work.
