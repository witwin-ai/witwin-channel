[build-system]
requires = [
    "cmake>=4.3,<4.4",
    "ninja>=1.13,<1.14",
    "scikit-build-core>=0.12,<0.13",
]
build-backend = "scikit_build_core.build"

[project]
name = "witwin-channel"
version = "0.4.0"
description = "GPU-accelerated differentiable wireless propagation simulation for RF digital twins."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = ["torch==2.10.0", "witwin==0.4.0"]

[tool.scikit-build]
wheel.packages = ["src/witwin"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "--import-mode=importlib"

[tool.mypy]
python_version = "3.11"
files = [
    "src/witwin/channel/capabilities.py",
    "src/witwin/channel/deployment.py",
    "src/witwin/channel/runtime.py",
    "src/witwin/channel/deterministic.py",
    "src/witwin/channel/montecarlo/basic.py",
    "src/witwin/channel/montecarlo/bdpt.py",
    "src/witwin/channel/path.py",
]
follow_imports = "skip"
ignore_missing_imports = true
check_untyped_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
strict_equality = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
warn_unused_configs = true

[tool.coverage.run]
branch = true
source = ["src/witwin/channel"]

[tool.coverage.report]
precision = 2
