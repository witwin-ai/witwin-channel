from __future__ import annotations

from .config import Config
from .result import Result


def solve(scene: object, config: Config) -> Result:
    raise NotImplementedError(
        "witwin.channel_native.deterministic solver is reserved for a later phase"
    )
